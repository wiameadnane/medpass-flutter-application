rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/medical_files/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    // keep existing rules for other collections
  }
}

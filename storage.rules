rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/medical_files/{allPaths=**} {
      allow read: if true; // adjust as needed
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
